# ============================================================
# SA-ID Citizen App — Python Dependencies
# Pinned versions to prevent breaking changes
# Last updated: 2026-03-13
# ============================================================

# ── Core API Framework ───────────────────────────────────────
fastapi==0.111.0
uvicorn==0.29.0
starlette==0.37.2

# ── Data Validation ──────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1

# ── Authentication & Security ────────────────────────────────
PyJWT==2.8.0
passlib==1.7.4
bcrypt==4.1.3
python-jose==3.3.0
cryptography==42.0.5

# ── Database ─────────────────────────────────────────────────
psycopg2-binary==2.9.9       # PostgreSQL driver (Windows)
asyncpg==0.29.0              # Async PostgreSQL (Jetson)
SQLAlchemy==2.0.30           # ORM (optional layer)
alembic==1.13.1              # Database migrations

# ── HTTP Client ──────────────────────────────────────────────
httpx==0.27.0
requests==2.31.0

# ── Utilities ────────────────────────────────────────────────
python-dotenv==1.0.1         # .env file support
python-multipart==0.0.9      # File uploads (ID card images)
Pillow==10.3.0               # Image processing (ID card scan)
python-dateutil==2.9.0

# ── Security Scanning ────────────────────────────────────────
pip-audit==2.7.3             # Scan for vulnerable packages

# ── Testing ──────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0                # Test client for FastAPI

# ── Logging & Monitoring ─────────────────────────────────────
loguru==0.7.2

# ── Production (Jetson / Linux only) ─────────────────────────
# gunicorn==22.0.0           # Uncomment on Jetson
# jax==0.4.26                # Uncomment on Jetson (GPU)
# numpy==1.26.4              # Uncomment on Jetson

# ============================================================
# Install: pip install -r requirements.txt
# Audit:   pip-audit
# ============================================================
